using UnityEngine;
using System.Collections;

public class WayPoint : MonoBehaviour
{
    public float Radius = 2000;
    private void Update()
    {
        //transform.rotation = Quaternion.LookRotation(-CameraRoot.I.transform.position + transform.position)*Quaternion.Euler(0,90,0);

    }
}
